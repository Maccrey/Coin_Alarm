# 암호화폐 뉴스 크롤링 서버 (MSA 아키텍처)

MSA(Microservice Architecture) 기반의 암호화폐 뉴스 크롤링 서버입니다. 여러 사이트에서 뉴스를 수집하고 Supabase에 저장합니다.

## 아키텍처

이 프로젝트는 다음과 같은 마이크로서비스로 구성되어 있습니다:

1. **news-crawler**: 뉴스 웹사이트에서 데이터를 수집 (본문, 대표 이미지(imageUrl) 등 포함)
2. **news-cleaner**: 수집된 데이터를 정제하고 중복을 제거하며, imageUrl(대표 이미지) 필드를 항상 유지
3. **news-writer**: 정제된 데이터를 Supabase에 저장 (imageUrl → image_url로 변환하여 저장)
4. **scheduler**: 크롤링 작업을 주기적으로 실행 (2시간마다)
5. **logger**: 로그 수집 및 모니터링 대시보드 제공

## 기술 스택

- **언어**: Python 3.9
- **웹 크롤링**: BeautifulSoup, Requests
- **컨테이너화**: Docker, docker-compose
- **데이터베이스**: Supabase (PostgreSQL)
- **모니터링**: Flask 기반 대시보드
- **스케줄링**: cron

## 설치 및 실행

### 사전 요구 사항

- Docker 및 Docker Compose 설치
- Supabase 계정 및 프로젝트

### 환경 변수 설정

`.env` 파일을 생성하고 다음 변수를 설정합니다:

```
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key
```

### 실행 방법

```bash
# 도커 컨테이너 빌드 및 실행
docker-compose up -d

# 로그 확인
docker-compose logs -f
```

## 서비스 접근

- 로깅 대시보드: http://localhost:5001

## 크롤링 대상 사이트

현재 다음 사이트에서 뉴스를 수집합니다:

- Blockmedia (https://www.blockmedia.co.kr/news)
- CoinReaders (https://coinreaders.com/sub_view/news.html)
- Bloomingbit (https://bloomingbit.io/news)

## 폴더 구조

```
webcrawler/
├── docker-compose.yml     # 도커 구성 파일
├── shared/                # 서비스 간 공유 디렉토리
├── logs/                  # 로그 저장 디렉토리
├── news-crawler/          # 뉴스 크롤링 서비스
│   ├── Dockerfile
│   ├── requirements.txt
│   └── crawler.py
├── news-cleaner/          # 뉴스 정제 서비스
│   ├── Dockerfile
│   ├── requirements.txt
│   └── cleaner.py
├── news-writer/           # 뉴스 저장 서비스
│   ├── Dockerfile
│   ├── requirements.txt
│   └── writer.py
├── scheduler/             # 스케줄러 서비스
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── crontab
│   ├── run_crawler.sh
│   └── check_services.py
└── logger/                # 로깅 서비스
    ├── Dockerfile
    ├── requirements.txt
    └── app.py
```

## 데이터 흐름 및 저장 구조

- **news-crawler**: 각 뉴스의 본문(content), 대표 이미지(imageUrl), 관련 코인 등 필드를 포함하여 crawled*news*\*.json 파일로 저장
- **news-cleaner**: 광고/중복/짧은 뉴스 필터링 및 정제, imageUrl 필드를 항상 유지하여 cleaned*news*\*.json 파일로 저장
- **news-writer**: cleaned*news*\*.json을 읽어 Supabase news 테이블에 저장 (imageUrl → image_url로 컬럼명 변환)

### Supabase에 저장되는 데이터 예시

```json
{
  "id": "...", // news_hash
  "title": "...",
  "content": "...",
  "url": "...",
  "source": "...",
  "published_at": "...",
  "created_at": "...",
  "image_url": "https://...", // 대표 이미지
  "related_coins": ["BTC", "ETH"] // 관련 코인 배열
}
```

## Supabase 테이블 구조

### news 테이블

```sql
create table news (
  id text primary key,  -- news_hash 값
  title text not null,
  content text not null,
  url text not null unique,
  source text not null,
  published_at timestamptz not null,
  created_at timestamptz default now(),
  image_url text, -- 대표 이미지 URL
  related_coins text[] -- 관련 코인 배열
);
```

### news_coins 테이블

```sql
create table news_coins (
  id bigint generated by default as identity primary key,
  news_id text references news(id),
  coin_symbol text not null,
  created_at timestamptz default now()
);
```

## 로깅 및 모니터링

- 각 서비스의 로그는 `shared/` 디렉토리에 저장됩니다.
- 로그 대시보드는 `http://localhost:5000`에서 접근할 수 있습니다.
- 서비스 상태는 30분마다 체크됩니다.

## 개발 및 테스트

TDD(Test-Driven Development) 방식으로 개발을 진행하고 있습니다. 각 서비스에 대한 테스트 코드는 `tests/` 디렉토리에 위치할 예정입니다.

## 기여

버그 신고, 기능 요청 또는 풀 리퀘스트는 언제든지 환영합니다.

## 라이선스

이 프로젝트는 MIT 라이선스 하에 배포됩니다.
